// Hand-authored to match wrangler.toml bindings.
// Regenerate with `npm run types` after adding/removing bindings.

interface Env {
  RATE_LIMIT: KVNamespace;
  PAGESPEED_API_KEY: string;
  RESEND_API_KEY: string;
  SITE_URL: string;
}
